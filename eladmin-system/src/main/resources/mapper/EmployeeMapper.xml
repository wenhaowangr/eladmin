<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="me.zhengjie.modules.system.dao.mapper.EmployeeMapper">

    <select id="getEmployeeByName" resultType="me.zhengjie.modules.system.domain.entity.EmployeeDO">
        SELECT * FROM tbl_sms_employee_info WHERE name = #{name} AND state = 0
    </select>

</mapper>
