<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.unionpay.modules.system.dao.EmployeeMapper">

    <select id="getEmployeeByName" resultType="com.unionpay.modules.system.entity.EmployeeDO">
        SELECT id, name, user_id FROM tbl_sms_employee_info WHERE name = #{name} AND state = 0
    </select>

    <select id="getEmployeeById" resultType="com.unionpay.modules.system.entity.EmployeeDO">
        SELECT id, name, user_id FROM tbl_sms_employee_info WHERE id = #{id} AND state = 0
    </select>

    <select id="getAllEmployee" resultType="com.unionpay.modules.system.entity.EmployeeDO">
        SELECT id, name, user_id FROM tbl_sms_employee_info WHERE state = 0
    </select>
    <select id="getEmployeeByUserId" resultType="com.unionpay.modules.system.entity.EmployeeDO">
        SELECT id, name, user_id FROM tbl_sms_employee_info WHERE user_id = #{userId} AND state = 0
    </select>

</mapper>
